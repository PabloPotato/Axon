{
  "installCommand": "cd .. && npm install --prefix axon-engine && npm install --prefix axon-audit && npm install --prefix dashboard",
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
